#include<iostream>
using namespace std;

int main(){
    int i, j;
    int n;
    string s;
    string p;
    cin >> n;
    for(i=0;i<n;i++){
        cin >> s;
        for(j=0;j<s.length();j++){
            if(p.size()==0){
                p.append(s[j]);
            }
            else if(p==){

            }
        }
    }

    return 0;
}
